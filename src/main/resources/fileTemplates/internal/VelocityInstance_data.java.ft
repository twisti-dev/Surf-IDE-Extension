package ${PACKAGE};

import ${CORE_INSTANCE_PACKAGE}.${CORE_INSTANCE_NAME};
import ${VELOCITY_MAIN_PACKAGE}.VelocityMain;

public final class ${VELOCITY_INSTANCE_NAME} extends ${CORE_INSTANCE_NAME} {

  public ${VELOCITY_INSTANCE_NAME}() {
    super();
  }

  @Override
  protected ClassLoader getClassLoader() {
    return VelocityMain.getInstance().getClassLoader();
  }
}